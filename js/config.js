/**
 * Configuración del front-end.
 * Cambia API_URL si tu backend corre en otro host/puerto.
 * Por defecto el backend (carpeta /backend) corre en http://localhost:3000
 */
const CONFIG = {
    API_URL: "http://localhost:3000/api/inscripciones"
};

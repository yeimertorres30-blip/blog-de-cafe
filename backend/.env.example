# Copia este archivo como ".env" (en la misma carpeta backend/) y ajusta los valores.
# En Windows/Mac/Linux desde la terminal de VSCode: cp .env.example .env

# Puerto en el que corre el servidor backend
PORT=3000
